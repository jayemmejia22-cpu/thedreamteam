<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.thedreamteam">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Material.Light">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".StudentOneActivity"
            android:exported="false"
            android:label="Student 1"
            android:theme="@android:style/Theme.Material.Light" />

        <activity
            android:name=".StudentTwoActivity"
            android:exported="false"
            android:label="Student 2"
            android:theme="@android:style/Theme.Material.Light" />

        <activity
            android:name=".StudentThreeActivity"
            android:exported="false"
            android:label="Student 3"
            android:theme="@android:style/Theme.Material.Light" />

        <activity
            android:name=".StudentFourActivity"
            android:exported="false"
            android:label="Student 4"
            android:theme="@android:style/Theme.Material.Light" />

        <activity
            android:name=".StudentFiveActivity"
            android:exported="false"
            android:label="Student 5"
            android:theme="@android:style/Theme.Material.Light" />

    </application>

</manifest>